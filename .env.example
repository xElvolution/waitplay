# Waitplay environment example
# Copy to .env.local for local overrides.

# Public app URL used in share copy helpers (optional)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Default agent latency multiplier (1 ≈ 30–45s multi-tool runs)
NEXT_PUBLIC_LATENCY_SCALE=1
